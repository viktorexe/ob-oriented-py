# Modules are packages (or in simple word very ling codes sometimes written in dfferent languages)
# which are useful in python for performing some operations like logging, math, date time 
# We can make our own module 

import math # importing a module
from math import sqrt # Import specific function
import math as m # Nickname 
print(math.sqrt(100))