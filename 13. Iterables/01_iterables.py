# Iterables are anything which we can use in a for loop, and their each element can be fetchced one by one
# list, tuple, set, variables 

something = 'Vansh'
for x in something:
    print(x)

# Something is an iterable 
